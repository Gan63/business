Sample PDF content for RAG testing.

This is a placeholder text document saved as PDF. In a real scenario, this would be an actual PDF with text that gets loaded, chunked, embedded into vectors, stored, retrieved based on queries, and augmented LLM prompts for generation.

Key concepts:
- Document loading
- Text chunking
- Embeddings
- Vector storage
- Retrieval
- LLM generation
